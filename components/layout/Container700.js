import { Container } from "react-bootstrap"

function Container700({ children }) {
    return (
        <Container className="container700">
            {children}
        </Container>
    )
}

export default Container700